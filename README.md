# BarPass

Monorepo con:

- apps/backend
- apps/frontend
- packages/shared

Comandos:

- `pnpm install` - instalar dependencias en el workspace
- `pnpm dev` - iniciar todos los servicios en paralelo (cuando estén configurados)
